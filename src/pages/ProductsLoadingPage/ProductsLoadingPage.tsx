import React, { memo } from 'react';

import styles from './productsLoadingPage.module.scss';

const ProductsLoadingPage = () => {
  return <div className={styles.pageWrapper}>ProductsLoadingPage</div>;
};

export default memo(ProductsLoadingPage);
