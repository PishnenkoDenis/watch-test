import React, { memo } from 'react';

import styles from './messagesPage.module.scss';

const MessagesPage = () => {
  return <div className={styles.pageWrapper}>MessagesPage</div>;
};

export default memo(MessagesPage);
