import React, { memo } from 'react';

import styles from './characteristicsPage.module.scss';

const CharacteristicsPage = () => {
  return <div className={styles.pageWrapper}>CharacteristicsPage</div>;
};

export default memo(CharacteristicsPage);
