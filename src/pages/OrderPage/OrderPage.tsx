import React, { memo } from 'react';

import styles from './orderPage.module.scss';

const OrderPage = () => {
  return <div className={styles.pageWrapper}>OrderPage</div>;
};

export default memo(OrderPage);
