import React, { memo } from 'react';

import styles from './commentsPage.module.scss';

const CommentsPage = () => {
  return <div className={styles.pageWrapper}>CommentsPage</div>;
};

export default memo(CommentsPage);
